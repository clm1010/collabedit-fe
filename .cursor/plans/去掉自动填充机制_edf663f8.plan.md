---
name: 去掉自动填充机制
overview: 删除 CollaborationPanel.vue 中的 watch 自动填充逻辑，只保留滚动触底加载。
todos:
  - id: remove-auto-fill
    content: "CollaborationPanel.vue: 删除 watch 自动填充 + scrollContainerRef + 相关 import"
    status: pending
isProject: false
---

# 去掉自动填充机制

只改 [CollaborationPanel.vue](e:\job-project\collabedit-fe\src\lmComponents\collaboration\CollaborationPanel.vue) 一个文件。

### 删除内容

1. **第 174-186 行**：删除整个 watch 块
2. **第 172 行**：删除 `scrollContainerRef` ref 声明
3. **第 68 行**：模板上删除 `ref="scrollContainerRef"`（只保留 `@scroll`）
4. **第 123 行**：import 中移除 `watch` 和 `nextTick`（`ref` 也不再需要）

改完后 script 部分不再需要从 vue 引入任何东西（原有的 props/emit/computed 都是编译器宏）。
