---
description: TypeScript 类型命名与组织规范。修改类型定义文件时自动加载。
globs: src/types/**, src/**/*.d.ts
---

# 类型命名规范

## 命名约定

- 业务实体类型用 **`VO` 后缀**：`TemplateVO`、`ExternalUserVO`、`TrainingPlanVO`
- 分页参数：`PageParam`
- 分页结果：`PageResult<T>`
- 枚举：`enum XxxEnum` + 配套文案映射 `XxxTextMap: Record<string, string>`

## 文件组织

- 类型文件按业务域组织在 `src/types/` 下：`management.ts`、`performance.ts` 等
- 全局类型扩展放 `src/types/*.d.ts`
