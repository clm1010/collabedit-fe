---
description: API 层与认证体系规范。修改 API 调用、Axios 配置或权限相关代码时自动加载。
globs: src/api/**, src/config/axios/**, src/utils/auth.ts, src/permission.ts
---

# API 层与认证体系

## API 封装

- 按业务模块分文件（`src/api/training/index.ts`），统一命名导出
- 使用 `javaRequest`（来自 `config/axios/javaService.ts`）：`get`/`post`/`postOriginal`/`upload`/`download`
- Mock/Java 后端通过 `USE_MOCK` 切换
- 视图级独立 API 放 `views/{module}/api/` 下

## Axios 实例分层

- `service.ts` — 主实例 + 拦截器
- `javaService.ts` — Java 后端专用实例
- `refreshToken.ts` — Token 刷新队列，根据 `VITE_BACKEND_TYPE` 区分 Java/Node 刷新接口

## 错误处理

- 错误码映射在 `config/axios/errorCode.ts`
- 401：`handle401` 使用请求队列暂存并发请求，等 Token 刷新完成后重发
- 403/405：`handleAuthorized` 处理

## 权限控制

- 模板指令：`v-hasPermi`（控制元素显隐）
- 逻辑判断：`checkPermi()`/`checkRole()`（来自 `utils/permission.ts`）

## 路由守卫

- 守卫在 `permission.ts` 中（非 `router/index.ts`）
- 支持 `VITE_SKIP_AUTH` 跳过认证
- 外部 Token 模式：`VITE_EXTERNAL_TOKEN_LOGIN`，URL 参数 `token`/`refreshToken`，白名单 `externalAllowedPaths`

## 关键环境变量

`VITE_BASE_URL`、`VITE_API_URL`、`VITE_WS_URL`、`VITE_BACKEND_TYPE`、`VITE_SKIP_AUTH`、`VITE_EXTERNAL_TOKEN_LOGIN`
