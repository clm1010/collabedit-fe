---
description: Pinia 状态管理与 Composables 规范。修改 store 或 hooks 时自动加载。
globs: src/store/**, src/hooks/**
---

# 状态管理与 Composables

## Pinia Store

- 使用 `pinia-plugin-persistedstate` 持久化
- **双入口模式**：每个 store 必须导出两个入口：
  - `useXxxStore` — 在 `<script setup>` 内使用
  - `useXxxStoreWithOut` — 在路由守卫、Axios 拦截器等 setup 外使用，通过 `useXxxStore(store)` 传入 store 实例
- Store 按业务拆分：`user`、`permission`、`dict`、`externalUser`、`collaborationUser`、`docBuffer` 等

## Composables / Hooks

- 通用 composables 放在 `src/hooks/web/` 目录
- 已有的 hooks（优先复用，避免重复造轮子）：
  - `useTable` — 表格逻辑
  - `useCache` — 缓存
  - `useI18n` — 国际化
  - `useValidator` — 校验
  - `useMessage` — 消息提示（ElMessage/ElNotification 封装）
  - `useLocale` — 语言切换
  - `useNProgress` / `usePageLoading` / `useTitle` — 页面状态
- 新增 composable 时放入 `hooks/web/` 目录

## 自动导入

以下通过 `unplugin-auto-import` 自动导入，无需手动 import：
`useI18n`、`useMessage`、`useTable`、`useCrudSchemas`、`required`、`DICT_TYPE`
